## Packages
recharts | For dashboard analytics charts (sales trends, order stats)
date-fns | For formatting dates in tables (e.g. order dates)
framer-motion | For smooth page transitions and micro-interactions

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["Inter", "sans-serif"],
  display: ["Outfit", "sans-serif"],
}
Authentication uses Replit Auth (provided via blueprint).
API endpoints are defined in @shared/routes.
